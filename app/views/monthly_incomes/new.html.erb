<% content_for :title, "Add income" %>

<div class="w-full max-w-lg mx-auto">
  <h1 class="mb-8 text-2xl font-semibold text-gray-900">Add income</h1>

  <%= render "form", monthly_income: @monthly_income %>

  <div class="mt-6">
    <%= link_to "← Back to dashboard", root_path(month: @monthly_income.month), class: "text-sm font-medium text-gray-600 hover:text-gray-900" %>
  </div>
</div>

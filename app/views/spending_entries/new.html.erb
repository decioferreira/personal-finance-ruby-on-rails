<% content_for :title, "New spending entry" %>

<div class="w-full max-w-lg mx-auto">
  <h1 class="mb-8 text-2xl font-semibold text-gray-900">New spending entry</h1>

  <%= render "form", spending_entry: @spending_entry %>

  <div class="mt-6">
    <%= link_to "← Back to spending entries", spending_entries_path, class: "text-sm font-medium text-gray-600 hover:text-gray-900" %>
  </div>
</div>
